<h2><%=h @blog.dog.name %>'s new blog post</h2>

<%= error_messages_for :post %>

<%= render :partial => 'blog_form',
           :locals => { :url => blog_posts_path(@blog),
                        :method => :post } %>
